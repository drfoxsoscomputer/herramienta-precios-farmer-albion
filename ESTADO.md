# ESTADO — Albion Helper

> Una pagina que cualquier sesion futura lee primero. Si esto no esta
> actualizado, es un bug mio, no del usuario.

## Que es
Helper de consola (Python + Rich) para Albion Online: consulta precios de
mercado de la API albion-online-data.com y recomienda donde vender
(entero vs picado) por ciudad.

## Como correr
```
python albion_helper.py          # consola
python -X utf8 flask_app.py      # web (8081)
python -X utf8 app.py            # app de escritorio: Flask + ventana webview maximizada (recomendado)
python -X utf8 build.py          # empaquetado portable (PyInstaller onedir)
```
Windows. Ya NO requiere consola compatible con VT: desde el fix del 2026-08-05
el render por filas usa la API nativa Win32 en consolas legacy (conhost sin
VT); con Windows Terminal / cmd moderno sigue usando ANSI (truecolor).
Distribución portable: `dist/AlbionHelper-portable.zip` (54.7 MB, sin
instalar nada — Python incluido). El exe principal abre la app en una ventana
tipo app maximizada (pywebview/WebView2) SIN lanzador tkinter ni bandeja.

## Repo GitHub (publico)
- https://github.com/drfoxsoscomputer/herramienta-precios-farmer-albion
- Nombre elegido con skill de copywriting: "La Herramienta de Precios del Farmer en Albion" (herramienta + precios + farmer + Albion)
- README en espanol con disclaimer de marca (proyecto no oficial de fans; "Albion Online" es marca de Sandbox Interactive)

## Estructura
- `albion_helper.py` — entry point (llama a menus.menu_principal)
- `menus.py` — toda la UI: selector con flechas, menus, detalles, colores por tier
- `constants.py` — ciudades, REF_MAP, COLORES_TIER, etc.
- `api.py` — get_prices/get_history con cache 60s y backoff ante 429
- `formatting.py` — helpers puros de formato (precios, colores, historial, resumen_ciudad)
- `catalogo.py` — buscador global: descarga items.json -> catalog.json (una vez, junto a la app) y busca por nombre ES-ES + UniqueName normalizados (tokens AND, dedupe @1..@4, tipos diario/arma/simple)
- `textos.py` — reseñas de ayuda (RESENAS_MENU, RESENAS_DETALLE, LEYENDA_TIERS, CALIDADES)
- `albion_config.json` — datos: pescados (38), recursos (fibra/madera/cuero/mineral/piedra), salsas con recetas
- `lanzador.py` — lanzador desktop LEGADO (tkinter+pystray); ya NO es entry del exe (se dejo de lado por el usuario, se conserva en el repo)
- `app.py` — ENTRY del exe portable: levanta Flask y abre la ventana tipo app maximizada (webview/WebView2); al cerrar la ventana apaga el server
- `generar_splash.py` — genera `splash.png` (banner estilo Albion dibujado 100% con PIL + fuente Cinzel; obra propia, sin assets del juego)
- `splash.png` — splash de arranque embebido en AlbionHelper.exe via `--splash` (PyInstaller); NO va junto al exe (va dentro)
- `build.py` — empaquetado portable (PyInstaller onedir, 2 exes + datos)
- `static/` — manifest.json + sw.js (PWA)
- `openspec/config.yaml` — config SDD (contexto, strict_tdd: false, comando de tests, reglas por fase)
- `openspec/specs/` + `openspec/changes/archive/` — estructura SDD (fuente de verdad, versionada en git)
- `ESTADO.md` — este archivo (memoria viva del proyecto)

## Estado actual
- Protocolo de continuidad global activo (AGENTS.md): ESTADO.md + CodeGraph + responsabilidad de subagentes
- **SPLASH DE ARRANQUE + INSTANCIA UNICA (2026-08-21)**: el exe muestra un splash nativo de PyInstaller mientras carga (antes: 3-5s de nada, riesgo de doble clic → doble instancia → conflicto de puerto 8081). (1) `generar_splash.py`: dibuja `splash.png` 640x400 con PIL a escala 2x (gradiente fondo #241c16→#120d0a + resplandor cálido + viñeta; placa metálica con esquinas biseladas #4a3826→#1f150d; título "ALBION HELPER" en Cinzel wght=700 con gradiente oro #f7e08a→#b98a3e vía máscara; ornamento línea-rombo-línea; "CARGANDO" espaciado; versión abajo-derecha). Estética inspirada en el banner de loot del juego pero 100% dibujo propio (la paleta no tiene copyright; Cinzel es SIL OFL redistribuible; se descargó `static/fonts/Cinzel-VF.ttf` variable + `Cinzel-OFL.txt` — PIL no lee woff2). (2) `build.py`: `run_pyinstaller(..., splash=)` agrega `--splash splash.png` SOLO a AlbionHelper.exe + REINTENTA 3 veces (sleep 6s) porque Defender a veces bloquea ucrtbase.dll recién copiado durante COLLECT con PermissionError transitorio. (3) `app.py`: `_instancia_duplicada()` con mutex de Windows (`CreateMutexW` vía `ctypes.WinDLL('kernel32', use_last_error=True)` + `ctypes.get_last_error()==183`; NUNCA `windll.kernel32.GetLastError()` que devuelve valores viejos); si hay otra instancia: cierra su PROPIO splash → MessageBox "ya está en ejecución" → sale limpio; el mutex se libera solo al morir el proceso (sin locks pegados). `_cerrar_splash()` (import guard de pyi_splash, no-op en dev) enganchado a `events.shown` de AMBAS ventanas + timer daemon de seguridad 15s. VERIFICADO E2E exe real: mecanismo mutex (checkers en procesos frescos: True con A vivo / False tras muerte), smoke dev (dup False, cerrar_splash no-op), build OK, A arriba /status 200 → B bloqueado vivo en diálogo (#32770 "Albion Helper") → WM_CLOSE → B exit 0 → A sigue 200. Zip regenerado (54.7 MB, creció por runtime Tcl/Tk del splash). GOTCHAS: cloudflared.exe corriendo desde dist mata el build (matarlo antes); Defender deja locks transitorios hasta en dist fresco (borrar dist y reintentar).
- **FLUJO NUEVO CON VENTANA DE INICIO + BANDEJA (2026-08-19, fixes aplicados 2026-08-20)**: `app.py` reescrito. Al abrir el exe levanta Flask (hilo daemon) y abre una ventana CHICA (420x560) con el launcher (`/launcher`, templates/launcher.html, tema Albion) que pregunta qué hacer: (1) **Abrir la app** → maximiza la MISMA ventana y carga `/`; (2) **Solo servidor** → OCULTA la ventana principal y muestra una ventana SEPARADA de SOLO QRs (`/qr-solo`, templates/qr_solo.html: los 2 QRs + URLs + estado del túnel, SIN menú ni config), queda en bandeja; (3) **Consola** → abre AlbionHelperConsole.exe (terminal VISIBLE, sin CREATE_NO_WINDOW) y CIERRA TODO lo demás (server, ventanas, bandeja — la consola es proceso aparte y sobrevive). **REGLA NUEVA: la app NUNCA abre el navegador** — `webbrowser` ELIMINADO de app.py; `_mostrar_ventana(url)` muestra/restaura/maximiza la ventana viva y carga la URL (crea la ventana solo si no existe). Bandeja pystray: Abrir app → `_mostrar_ventana(APP_URL)`, Ver QR → `_mostrar_ventana_qr()` (antes iba a /config), Túnel con LABEL DINÁMICO (callable pystray re-evaluado en cada apertura del menú: "Túnel: Activo"/"Túnel: Inactivo") + CHECK nativo (checked=lambda) + ícono con ANILLO de color que cambia según estado real (`_actualizar_icono_bandeja()`: verde #2ecc71 activo / amarillo #c9a227 apagado, vía `_tray.icon`), Salir (destruye AMBAS ventanas + /shutdown + os._exit). Flag `--server` = ventana de SOLO QRs visible + bandeja (equivalente a opción 2, sin navegador). Al cerrar la ventana principal pregunta SIEMPRE (MessageBoxW nativo vía ctypes, evento pywebview `closing` CANCELABLE): **Sí** = cerrar por completo (destruye AMBAS ventanas para que webview.start() retorne y main() apague el server), **No** = minimizar a bandeja (server sigue), **Cancelar** = seguir. La ventana de solo-QR al cerrar con la X se OCULTA a la bandeja (nunca cierra la app). js_api `LauncherApi`: `abrir_app`/`solo_servidor`/`abrir_consola`. `/shutdown` (os._exit) apaga todo. Botón "Cerrar aplicacion" ELIMINADO de config.html (elemento + listener). flask_app.py: nueva ruta `GET /qr-solo` (tunel_url + url_local, renderiza qr_solo.html con `_contexto`); `/status` devuelve `{status, tunel_activo, url}`. build.py: hidden_imports += pystray + PIL. VERIFICADO: py_compile OK, dev (--server: /qr-solo 200 con QR y sin menú, /launcher 200, proceso estable, SIN procesos de navegador) + exe empaquetado (AlbionHelper.exe 9.1 MB, --server /qr-solo 200 con QR, modo normal /launcher 200 con pywebview.api, sin navegador). Zip regenerado (52.9 MB). GOTCHA build: si cloudflared.exe corre desde dist\AlbionHelper (túnel activo), PyInstaller falla con PermissionError al borrar la carpeta → matar cloudflared antes de build. GOTCHA UX (2026-08-20): el menú nativo de bandeja en Windows NO pinta emoji en color (monocromo) → el estado del túnel se indica con texto+check+ícono de bandeja, no con 🟢/🔴. PENDIENTE: prueba manual del usuario en la PC real (ventana launcher → 3 modos → diálogo de cierre → bandeja → túnel on/off con estado → salir) y confirmar que "Solo servidor" mostrando el QR en la ventana es lo esperado.
- **SDD inicializado (2026-08-01)**: modo hybrid (openspec + engram); strict_tdd: false (no hay pytest); artifact store "both"; PRs force-chained; review budget 400 líneas
- **SDD v2 — BASE COMPLETADA (2026-08-01, slice apply 1)**: baterias de tests migradas de Temp a `tests/` (regression_fase2.py 12 secciones + test_api_cache.py 4 tests); `test_command` en openspec/config.yaml (apply y verify) apunta a `tests/`; ambos PASS desde la raiz con `python -X utf8`
- **SDD v2 — WORK UNIT 2 COMPLETADO (2026-08-01, slice apply 2, PR 2)**: fase A (10 items T8 en albion_config.json + tests/test_recursos_t8.py) y fase B (get_history_raw en api.py, market_summary pura en formatting.py, resumen "Resumen de mercado" en los 3 detalles de menus.py, copy neutral en textos.py, tests/test_market_summary.py). El bloque de recomendacion picar/entero fue ELIMINADO. `test_command` ahora corre las 4 baterias; 340 lineas de diff (dentro del presupuesto ~345)
- **SDD v2 — FIX UI (2026-08-01)**: barra de seleccion restaurada a CYAN (estilo de la fase de flechas, memoria #150) — el commit 6281f4f (00:07) la habia cambiado a color-del-tier/blanco; items no seleccionados sin dim; reseña del detalle de pez sin la frase "sin recomendaciones" (texto de mas); `_COLORES_OSCUROS` eliminado (codigo muerto); test regression_fase2.py seccion 8 actualizado al contrato cyan (4 baterias PASS)
- **SDD v2 — FIX POST-VERIFICACION coherencia (2026-08-01)**: coherencia total "sin recomendaciones" + ciudades en el resumen de mercado. `color_item(val, todos)` sin es_bajo (verde = mayor precio, rojo = menor, dato neutro); `market_summary` agrega `min_ciudad`/`max_ciudad` (primer match por ciudad); panel "Observacion" -> "Precio mayor por ciudad" (siempre modo max, sin "comprar/vender"); panel salsas sin veredicto CONVIENE/VENDER INSUMOS (solo numeros); _panel_resumen muestra la ciudad: "Venta min: $X (Ciudad)"; 5 reseñas reescritas en textos.py (incluye RESENAS_MENU["insumos"] que el plan no listaba pero violaba el grep de copy); `es_bajo`/`tier_num` eliminados de _ver_detalle_recurso; tests ciudad min/max en escenarios 1-2. 4/4 baterias PASS + checks de contrato OK
- Contexto persistido en Engram: `sdd-init/albion` (#153), `sdd/albion/testing-capabilities` (#154), `skill-registry` (#155)
- CodeGraph indexado en `.codegraph/` (6 archivos, 105 nodos) — regenerar con `codegraph update` tras cambios estructurales
- Selector con flechas + numeros en todos los menus (grid 2 columnas en pesca/recursos)
- Navegacion unificada: flechas mover, Enter elegir, F5 recargar (global), Esc volver (raiz: confirmacion para salir)
- Render SIN parpadeo: diferenciador de lineas (reescribe solo filas cambiadas, ANSI puro)
- Seleccion con barra CYAN + texto negro (estilo fase de flechas; el commit 6281f4f lo habia cambiado a color-del-tier/blanco y fue revertido)
- Numero [ x] con el color de su label (ya no amarillo fijo)
- Truecolor forzado en el frame (T6 = naranja 208 real, no amarillo degradado)
- Item "Reiniciar" eliminado de la raiz (R ya recarga desde cualquier pantalla)
- Cache 60s en api.py contra rate limit 429; backoff 2s/4s
- **SDD v2 — SLICE APPLY 3 (2026-08-01)**: market_summary SIN historial (firma `market_summary(precios, item, recetas_config=None)`); eliminados volumen_total/dia_mayor_venta/volumen_dia del calculo, del dict de retorno y del panel de resumen (dato descartado por el usuario); panel de recursos ahora "Precio mayor y menor por ciudad" (mayor + menor fusionados por linea con su ciudad via `mejor_ciudad(vals, "min")`); copy de textos.py sin "dia de mayor venta"; tests/test_market_summary.py actualizado al nuevo contrato (5 escenarios); `get_history_raw` ya no se importa ni se usa en menus.py (sigue en api.py, sin callers)
- **SDD v2 — CAMPO "uso" DETALLE DE PESCA (2026-08-01)**: campo `uso` (nombre oficial ES del plato/trofeo) agregado SOLO a los 22 peces raros de albion_config.json y albion_config.full.json (los comunes NO llevan "uso"; las claves "_Tn" son separadores y se ignoran); `RESUMEN['uso'] = 'Se usa en'` en textos.py; `_panel_resumen` gana el parametro `uso=""` (si es truthy agrega la linea DESPUES del bloque ingrediente y ANTES de diferencia_refinado; la docstring documenta que uso="" oculta la linea); `ver_detalle_pez` lee `uso = config.get("pescados", {}).get(nombre, {}).get("uso", "")` y llama `_panel_resumen(resumen, uso=uso)` SIN mostrar_ingrediente (la linea "Es ingrediente de" era falsa en peces: el pez entero no participa de salsas); `ver_detalle_insumo` INTACTA (sigue con mostrar_ingrediente=True, salsas). tests/test_market_summary.py: +2 secciones (6: integridad config raro->uso presente, comun->uso ausente; 7: Tiburón T8 -> "Trofeo de tiburón" con tilde). 4/4 baterias PASS
- **ELIMINADA GANANCIA DE REFINAR (2026-08-01)**: borrado TODO el calculo de ganancia de refinar (era recomendacion velada, no dato de mercado): `_SUFIJOS_REFINADO`, `_diferencia_refinado()` y la clave `diferencia_refinado` del retorno/docstring de `market_summary` en formatting.py; el bloque de diferencia en `_panel_resumen` y la columna "Dif" de la tabla de recurso refinado en menus.py; `RESUMEN["diferencia"]` en textos.py; seccion 4 de tests/test_market_summary.py (renumeradas 5->4, 6->5, 7->6). Contrato market_summary queda: min_venta, max_venta, min_ciudad, max_ciudad, es_ingrediente, recetas, sin_datos. `color_signo` INTACTO (sigue en salsas). 4/4 baterias PASS (26 checks)
- **UI SALSAS: separacion + extra con signo/color (2026-08-01)**: `Group` de menu_insumos_pesca separa tabla / grid / volumen con `Text("")` (la info ya no queda pegada); el "extra" de cada salsa usa `{extra:+,}` y `{pct_extra:+.1f}` con `color_signo(extra)` (verde $+103 / rojo $-103 / amarillo 0). 4/4 baterias PASS
- **UI CONSISTENCIA headers + footer + sin extra (2026-08-01)**: (1) 4 headers unificados al mismo estilo `Panel(<reseña>, title="<seccion>", border_style="cyan", box=box.ROUNDED, expand=True)` — nombre de seccion sobre el borde, reseña ADENTRO (menu_principal "Albion Helper", menu_pesca "Pesca" con LEYENDA_TIERS dentro, ver_recurso "Recurso" con nombre del recurso en negrita + reseña + LEYENDA_TIERS dentro, menu_insumos_pesca "Salsas de pescado"); (2) footer del selector reescrito: descripcion del item suelta + linea en blanco + caja con SOLO el hint (Panel cyan ROUNDED expand, col_hint + flechas + Enter + Esc salir/volver + R), identico en todas las pantallas; (3) ELIMINADO el "extra" del grid de salsas (comparacion de rentabilidad = recomendacion velada): fuera carne_max_vta/alga_max_vta/alga_px/carne_px y valor_insumos/extra/pct_extra; la 3a columna del grid es solo `[bold]{ciudad_venta}[/] ${mejor_venta:,}`; el DETALLE de cada salsa CONSERVA su analisis (ver_detalle_insumo intacta); (4) volumen por ciudad dice "uds" ("Martlock: 50,912 uds"); (5) limpieza: parametro muerto `texto_bajo` eliminado de `_menu_seleccion` (firma + 4 call sites + helper run_sel del test). 4/4 baterias PASS (26 checks) + smoke render de los 4 headers OK
- **UI PARES CRUDO/REFINADO (2026-08-01)**: menu principal (opciones 2-6) y encabezado de ver_recurso muestran cada recurso como par "crudo/refinado" (Fibra/Tela, Troncos/Tablas, Piel/Cuero, Mineral/Barra, Piedra/Bloque) via `PARES_RECURSO` en textos.py (claves = claves de config, con fallback a nombre_recurso). El encabezado de ver_recurso ya NO repite el nombre adentro: el par va sobre el borde (title) y el body queda reseña + LEYENDA_TIERS (misma anatomia que los otros 3 headers unificados). Sin cambios de logica ni de formatting.py. 4/4 baterias PASS + smoke render de los 5 headers OK
- **UI SALSAS: layout "listado arriba" (2026-08-01)**: menu_insumos_pesca ahora usa `_menu_seleccion` con el nuevo parametro `titulo_abajo`: el panel "Salsas de pescado" va ARRIBA (titulo), el listado [1][2][3] va inmediatamente despues, la descripcion/receta de la salsa seleccionada (dinamica) va ENTRE el listado y los datos, y el bloque inferior (tabla de mercado + grid recetas + volumen 7d) va DESPUES (titulo_abajo); el footer queda solo con la caja de hint. Con `titulo_abajo` activo, `footer(con_desc=False)` no imprime la descripcion abajo. Sin `titulo_abajo` (default) el resto de menus mantiene el comportamiento historico (desc en footer). 4/4 baterias PASS + smoke render navegando (receta cambia arriba)
- **UI DETALLES UNIFICADOS (2026-08-01)**: los 3 detalles (ver_detalle_pez, _ver_detalle_recurso, ver_detalle_insumo) pasan al estilo de header/footer de los menus: header = Panel(nombre del item en el borde, reseña adentro, cyan ROUNDED expand) + footer = nuevo helper `_hint_detalle()` (Panel "Esc volver · R recargar", cyan ROUNDED expand) que reemplaza los 6 hints sueltos "[Esc] Volver · [R] Recargar". El tag "T6 Comun — 8 trozos al picar" va como primera linea del cuerpo del panel de pez. El caso "sin datos" de menu_insumos_pesca tambien pasa a Panel + hint. 4/4 baterias PASS + smoke render de los 3 headers OK
- **UI HEADER CON COLOR DE PALETA (2026-08-02)**: el header de las 3 pantallas de detalle ahora se dibuja con el COMPONENTE UNICO `_panel_detalle(nombre, color, contenido)` (menus.py, junto a `_hint_detalle`) y usa el MISMO color de la paleta con que el item se lista en el menu: pez -> `info_tier()` (COLORES_TIER), recurso -> `COLORES_TIER[tier_num]` (antes `[bold cyan]` FIJO), salsa -> `ENCH_COLORS[nivel]` (antes `[bold cyan]` FIJO). El titulo y el tag "T4 Comun" van SIN negrita y el tag FUERA del `[dim]` (la negrita en terminales de 16 colores aclaraba el color: `\x1b[1;36m` vs `\x1b[36m` puro del menu). Renders ANSI verificados: pez T4=36, T8=33, recurso T5=35, salsa L2=34 — IDENTICOS al menu. 4/4 baterias PASS. Cambiar la paleta en constants.py propaga el color a menu Y detalle.
- **ENCANTAMIENTOS SOLO T4+ (2026-08-02)**: en `_ver_detalle_recurso`, los encantamientos `.1-.4` (T{tier}_LEVEL{i}@{i}) existen en Albion desde T4; T2/T3 NO tienen versiones encantadas. Nuevo flag `has_ench = int(tier_key[1:]) >= 4`: si es False se omiten las columnas `.1 .2 .3 .4` de ambas tablas (crudo/refinado), no se consultan los ids `_LEVEL*` a la API y el panel "Precio mayor y menor por ciudad" no lista niveles 1-4. Verificado con renders: T2 (Algodon) tabla solo `Ciudad | Algodon`; T4 (Cañamo) muestra `.1-.4` con datos. 4/4 baterias PASS.
- **TABLA RECURSO UNIFICADA (2026-08-02)**: el detalle de recurso en modo "todo" ya NO dibuja dos tablas apiladas (una para el crudo y otra para el refinado, con la columna `Ciudad` duplicada). Ahora es UNA sola tabla: columna `Ciudad` unica + columnas del crudo (con sus encantamientos) + columnas del refinado (con sus encantamientos), segun `modo` y `has_ench`. Verificado con renders: T2 -> `Ciudad | Algodon | Tela simple`; T4 -> `Ciudad | Cañamo | .1..4 | Tela fina | .1..4` (una fila por ciudad). 4/4 baterias PASS.
- **VOLUMEN 7 DIAS: ancho dinamico (2026-08-02)**: en `_formatear_historial` (formatting.py) la columna de volumen deja el ancho fijo `>8,` y pasa a ancho DINAMICO (`ancho_vol` = max de los volumenes incl. el Total) para que "uds" y el promedio queden SIEMPRE alineados, aun con totals de 9+ caracteres (ej. `1,715,202`). Antes el fijo `>8` desplazaba el " uds" cuando el numero superaba 8 digitos -> el Total quedaba desalineado. Verificado: pos de "uds" = 30 en todas las filas (ciudades + Total). 4/4 baterias PASS.
- **PANELES ENCOGIDOS (2026-08-02)**: los cuadros que se estiraban a todo el ancho de la pantalla ahora se encajan a su contenido con `expand=False` (4 paneles "Volumen 7 dias": pez, recurso, salsas y detalle de salsa; el "Resumen de mercado"). El resumen de mercado de un recurso en modo "todo" muestra crudo Y refinado juntos en una grid alineada [item | venta min | venta max] via `nombre_principal` + `items_extra` de `_panel_resumen` (los modos clásico/refinado de recursos y los resums de peces/salsas siguen con las filas clasicas "Venta min:/max:"). 4/4 baterias PASS + smoke renders T2/T3 sin ench y T4 con ench OK.
- **ELIMINADO PANEL DUPLICADO + DESEMPATE POR VOLUMEN (2026-08-02)**: borrado el panel "Precio mayor y menor por ciudad" (duplicaba el "Resumen de mercado" con otro orden, confundía al usuario mostrando ciudades distintas ante empate). Queda UN SOLO resumen. `market_summary` gana el parametro `volumen={ciudad: volumen}`: cuando varias ciudades empatan en el precio minimo (o maximo), la de MAYOR volumen de venta 7d desempata (si ninguna tiene dato, queda la primer match del dict). Helper `_volumen_por_ciudad(hist)` en menus.py extrae {ciudad: volumen} del historial; se pasa en recursos (todo/crudo/refinado), pez (entero) y salsas. Eliminada `_linea_mayor_menor` (muerto). Verificado: 3 ciudades a $120 -> el resumen muestra Lymhurst (40,000 uds) en vez de aleatoria. 4/4 baterias PASS + test 7 (desempate por volumen) en test_market_summary.py.
- **RESUMEN DE SALSA SIN LINEA DE INGREDIENTE (2026-08-02)**: en ver_detalle_insumo (menus.py) se quito `mostrar_ingrediente=True` al llamar `_resumen`. La salsa es el producto final y NUNCA es ingrediente de otra salsa, por eso la linea "No es ingrediente de ninguna salsa" siempre salia (dato basura que confundia). Ahora el resumen de la salsa deja claro el resumen SOLO con Venta min/max. Peces y recursos no se tocan. 4/4 baterias PASS + smoke render del detalle de salsa (sin la linea).
- **DETALLE DE SALSA: "Donde vender" reemplaza el "Analisis de rentabilidad" (2026-08-02)**: borrado TODO el panel "Analisis de rentabilidad" del detalle de salsa (ver_detalle_insumo): el calculo de costo de insumos asumia que el usuario COMPRA los ingredientes al menor precio y calculaba ganancia contra ese costo — falso para un farmer que pesca todo (no hay costo, puso trabajo). Eliminado: costo total, ganancia/perdida con %, la tabla "vs vender insumos por separado" y el "TOTAL por salsa". Ahora queda un panel compacto "Donde vender": receta (con espacios alrededor del simbolo ×) + mejor ciudad de venta por MAYOR PRECIO y, ante empate, la de MAYOR VOLUMEN 7d + su volumen. Se consulta el historial UNA vez y se reusa para el panel "Volumen 7 dias". Limpieza: eliminados `_tabla_insumos` (muerto), import `pct`/`color_signo`; reseñas de textos.py (RESENAS_DETALLE["insumo"] y RESENAS_MENU["insumos"]) reescritas. 4/4 baterias PASS + smoke render.
- **DETALLE DE SALSA: QUITADO "Donde vender", receta neutra (2026-08-02)**: el panel "Donde vender" (que decia "Vender en X: $") fue ELIMINADO porque era una RECOMENDACION disfrazada (instruia una accion de venta), contradiciendo la regla de solo-datos. Quedo el detalle como dato neutro: receta en linea simple (sin panel, sin mandato) + tabla Ciudad|Alga|Carne|Venta + Volumen 7 dias + Resumen de mercado (Venta min/max con ciudad, desempate por volumen). Resena de textos.py actualizada (RESENAS_DETALLE["insumo"] y RESENAS_MENU["insumos"]) a copy neutro. 4/4 baterias PASS + smoke render neutral.
- **COLUMNA "Actualizado" + RESEÑAS SIN META-DISCURSO (2026-08-02)**: las 3 tablas de detalle (pez Entero/Picado, recurso crudo/refinado+ench, insumo Alga/Carne/Venta) ganan la columna "Actualizado" a la DERECHA con la frescura de la fila en tiempo relativo ("hace 10 s"/"hace 5 min"/"hace 3 h"/"hace 2 d"; singulares "hace 1 min/h/d"; dias solo desde 24 h). Nuevo helper puro `antiguedad(iso, ahora=None)` en formatting.py (datetime.fromisoformat, naive=UTC, None/basura -> "", clamp a 0 s para timestamps futuros). En menus.py los timestamps de la API (sell_price_min_date/max_date) se preservan en un dict paralelo `fechas` (el contrato de `prices`/`prices_map`/`precios_grp` para market_summary NO cambia: siguen siendo ints) y el helper `_fecha_fresca(fechas, items, ciudad)` elige el ISO mas reciente de la fila (max lexico); fila sin datos -> "—". api.py NO cambia (get_prices ya devuelve la lista cruda con los timestamps). RESENAS_DETALLE en textos.py pierden las colas "muestra datos objetivos"/"sin recomendaciones" (queda lo informativo). Bateria nueva tests/test_antiguedad.py (6 secciones: unidades, bordes 59/60, desfase futuro, invalidos, formatos Z/offset/datetime, frescura max). 5/5 baterias PASS + smoke render de los 3 detalles con timestamps fake.
- **FIX CENTINELA API en "Actualizado" (2026-08-02)**: la API de Albion manda `0001-01-01T00:00:00` (anio 1) como timestamp en ciudades SIN ventas (verificado con llamada real: Black Market/Bridgewatch/Caerleon/Thetford en salsa N/D). La columna "Actualizado" calculaba "hace 739830 d" en filas N/D. Fix doble: (1) el mapeo en menus.py (3 sitios: pez, recurso, insumo) SOLO guarda timestamps si `sell_price_min > 0` o `sell_price_max > 0`; (2) guardia defensiva en `antiguedad`: `fecha.year < 2000 -> ""`. Test nuevo en test_antiguedad.py seccion 4 (centinela -> ''). 5/5 baterias PASS + smoke render con API real (N/D -> "—", ciudades con datos -> "hace X h").
- **MIGRACION WEB FLASK (2026-08-13)**: migración de NiceGUI a Flask+HTMX+Tailwind. Dashboard con tarjetas a secciones, menú persistente arriba, pantalla Config con URL LAN + QR para celular. Ajustes de UI: badge "Celular: http://..." removido del dashboard; footer simplificado a "Datos de la API de Albion Online"; botón ← Volver a Inicio agregado a las pantallas de lista (pesca, recursos); panel Volumen 7 dias rediseñado de líneas de terminal a tabla con columnas Ciudad | Volumen | Promedio | Rango | Cambio % (verde/rojo). Diagnóstico de red: usuario PC en LAN router (192.168.0.x) + celular en red ONU (distinta subred) → el IP LAN no llega al celular. Solución recomendada: túnel cloudflared quick tunnel (gratis, sin cuenta) para exponer la web a internet y que el QR funcione desde cualquier red, sin tocar la configuración física del usuario. Server Flask (flask_app.py) corriendo puerto 8081, NiceGUI (app.py) puerto 8080 intacto. Memory guard topic_key: `decision/ajustes-ui-redise-o-flask-dashboard-footer-botones-volumen-config`.
- **SDD v2 — FASE C BUSCADOR COMPLETADA (2026-08-05, slice apply 3, PR 3)**: buscador global con catálogo local. Nuevo `catalogo.py` (normalizar NFD sin acentos + buscar tokens AND contra nombre ES-ES/UniqueName, dedupe @1..@4 al base, tipos diario/arma/simple, máx 15, descarga única items.json -> catalog.json junto a la app con error amigable). `textos.py`: `CALIDADES` + reseñas "buscar"/"buscado". `menus.py`: opción 8 "Buscar" (índices 1-7 intactos), `menu_buscar` con escritura en vivo/Backspace (reusa `_leer_tecla`), `ver_detalle_buscado` con UNA llamada get_prices sin volumen/resumen (arma: 5 paneles base/.1..4 x 11 columnas; diario: Vacío/Lleno con {base}_EMPTY/_FULL; simple: 1 panel 5 calidades; verde/rojo por calidad, "—" sin datos). `formatting.py`: `resumen_ciudad(hist)` pura (promedio ponderado, rango min-max, cambio % actual vs promedio, None sin datos) y `_formatear_historial` pasa a datos CRUDOS de `get_history_raw` con `(promedio $X · rango min-max · ±Y%)` por ciudad (ancho dinámico + Total alineado); los 3 detalles (pez/recurso/salsa) usan `get_history_raw` sin duplicar llamadas. `menu_insumos_pesca`: ELIMINADO panel "Volumen 7 dias" + `get_history(salsa_ids)`; grilla de recetas solo receta (sin ciudad/precio). E2E regression_fase2 SIN cambios (no hardcodea nº de opciones). 6/6 baterías PASS (regression, api, t8, market_summary, antiguedad, catalogo) + smoke renders de los 3 tipos de detalle y menu_buscar. catalog.json en .gitignore (23MB regenerable).
- **FIX TECLA RECARGAR R->F5 (2026-08-05)**: la tecla "R/r" recargaba la app desde cualquier pantalla (rama `tecla.upper() == "R" -> reiniciar()` en selectores, pausa y buscador). Eso entra en conflicto con el buscador global (Fase C): escribir cualquier palabra con "r" (diario, recurso...) relanzaba la app porque la rama R ganaba ANTES que `isprintable()`. Fix global (opción elegida por el usuario: F5 en TODA la app): `_leer_tecla` ahora mapea F5 (`\x00`+`?`, scan-code estándar de msvcrt: F1=";", F5="?", F6="@") -> "f5"; `_menu_seleccion`, `_pausa_volver` y `menu_buscar` recargan con `tecla == "f5"`; callers `idx == "f5"` (raiz, pesca, recurso, insumos); hints "F5 recargar" (selector, detalle, buscador). R y r quedan LIBRES en toda la app: en el buscador se escriben como texto normal; en selectores se ignoran. tests/regression_fase2.py actualizado (f5 -> reiniciar; R ya no recarga). Validación final pendiente en consola real (el mapping de F5 es estándar pero no se puede probar en sandbox).
- **FIX RENDER ANSI CRUDO EN CONSOLA SIN VT (2026-08-05)**: el usuario reportó que el PRIMER arranque (doble clic a albion_helper.bat -> `start powershell -Command python`) mostraba códigos ANSI crudos (`←[36m` en bordes/selección) y tras R se veía bien. Causa raíz (verificada en git + Rich 14.2.0): el commit `4b9973f` (31 Jul, "navegación sin parpadeo") introdujo el render por filas con `_RawControl` (ANSI crudo `\x1b[{fila};1H...\x1b[K`); Rich NUNCA habilita VT (solo lo detecta en `get_windows_console_features`), y la consola conhost nueva del .bat arranca sin `ENABLE_VIRTUAL_TERMINAL_PROCESSING` -> el ANSI crudo se ve literal; antes de ese commit todo se dibujaba con `console.print` (renderer legacy de Rich, API Win32 nativa, sin ANSI). Fix elegido por el usuario (opción 2, sin dependencia de ANSI): `_escribir_linea_nativa` (menus.py) decodifica el ANSI del buffer con `AnsiDecoder` y escribe cada tramo con `LegacyWindowsTerm` (SetConsoleTextAttribute + WriteConsoleW) — se usa SOLO cuando `console.legacy_windows` AND `_consola_nativa()` confirma consola REAL (GetConsoleMode OK; en pipes -> None -> ANSI). `_escribir_fila` y nuevo `_escribir_frame_completo` bifurcan: legacy = nativo, con VT/Unix/redirección = ANSI actual (sin regresión truecolor T6). 6/6 baterías PASS + stub test de la lógica de spans (cursor fila + estilos + eol).
- **BUSCADOR: FIXES DETALLE/LISTADO (2026-08-05, 4 cambios aprobados)**: (1) `_limpiar_nombre` en catalogo.py case-insensitive — "(Parciallymente lleno)" con P mayúscula (diarios de trofeo) ya se limpia del nombre; test 4b en test_catalogo.py. (2) Columnas de calidad SOLO con datos (`_calidades_con_datos` en menus.py): el Alga (solo calidad Normal) ya no muestra 4 columnas vacías; aplica a arma y simple, diario intacto. (3) Reseña de detalle por tipo en textos.py (`buscado`/`buscado_arma`/`buscado_diario`): diarios ya no mencionan encantamientos. (4) Header del detalle: el ID crudo (T3_JOURNAL_STONE) reemplazado por tag de tier con color ("T3 Diario"/"T4 Arma"/"T1 Item"), mismo patrón de los otros detalles; el listado de resultados de buscar también pinta el nombre con el color del tier. 6/6 baterías PASS + py_compile. Commit `cce520d` + push.
- **BUSCADOR SIN PARPADEO (2026-08-05)**: `menu_buscar` redibujaba TODO el frame con `limpiar_pantalla()` + `_RawControl` en CADA tecla -> parpadeo igual al viejo del menú principal. Ahora usa el mismo patrón de `_menu_seleccion`: primer frame completo (`_escribir_frame_completo`), luego `_repintar_diff` (solo filas cambiadas). Al volver del detalle (`Enter`) se fuerza redibujo completo (`primera = True`) porque el detalle tapó la pantalla. Commit `5e1018e` + push. 6/6 baterías PASS.
+- **BUSCADOR: COLOR CURSOR (2026-08-05, validado en consola real)**: el resultado SELECCIONADO del buscador mantenía el color del tier dentro de la barra cyan -> ilegible. Ahora la selección es negro sobre cyan (mismo patrón del menú de pesca); el color del tier solo queda en los resultados NO seleccionados. Validado por el usuario junto con la tecla F5. Commit pendiente.
+- **MIGRACION WEB NICEGUI — FASE 1 (2026-08-05, sin commit)**: nueva capa web local que REUTILIZA toda la lógica de datos (api.py, formatting.py, textos.py, constants.py intactos). `app.py` (entry web: carga config, detecta IP LAN, ui.run host 0.0.0.0:8080, SOLO en __main__), `webui.py` (pantallas NiceGUI 3.15: inicio con 8 tarjetas + banner "Desde el celular: http://<IP>:8080", /pesca con color de tier, /pesca/{nombre} detalle fiel a ver_detalle_pez con spinner + tabla Ciudad|Entero|Actualizado|Picado|Actualizado + Volumen 7 dias + Resumen, /recursos/* /salsas /buscar placeholders Fase 2) y `colores_web.py` (adapter Rich->CSS: COLORES_TIER/ENCH_COLORS + precio_web verde/rojo). El config se inyecta con set_config() (evita imports circulares). GOTCHA: `ui.banner` NO existe en NiceGUI 3.15 (AttributeError en /) -> reemplazado por ui.card con style bg. Smoke: / 200, /pesca 200, /pesca/Arenque 200 (red real). 6/6 baterías PASS. py_compile OK, import de app/webui sin lanzar server.
- **MIGRACION WEB — FASE 1b REDISENO VISUAL (2026-08-05, sin commit)**: tema oscuro + dorado Albion estilo OneLifeGaming (pedido del usuario). Nuevo `estilo.py`: ui.colors (primary #c9a227 dorado, dark #121216/#1a1a1e) + CSS global (fondo con gradiente, .alb-card redondeadas con hover borde dorado, .alb-table con cabecera oscura dorada + zebra + hover, .alb-card-static). `app.py` llama estilo.aplicar_tema() antes de ui.run. `webui.py`: header con gradiente + borde dorado + logo sports_esports; inicio con hero (título dorado + badge IP redondeado) + grilla responsive de 8 tarjetas con iconos (water/eco/forest/checkroom/diamond/terrain/set_meal/search); pesca con tarjetas barra lateral color tier + badge T{tier}; detalle con spinner dorado, tabla alb-table, paneles Volumen/Resumen con iconos dorados y precios verde/rojo. Smoke: 3 rutas 200. 6/6 baterías PASS.
- **MIGRACION WEB — FIX TEMA (2026-08-05, sin commit)**: `python app.py` explotaba con "RuntimeError: ui.page cannot be used in NiceGUI scripts when UI is defined in the global scope". Causa: aplicar el tema con `ui.colors()` / `ui.add_css(shared=False)` creaba UI global que choca con @ui.page en NiceGUI 3.15. Fix: `estilo.aplicar_tema()` ahora usa SOLO `ui.add_css(PALETA_CSS + CSS_GLOBAL, shared=True)` — PALETA_CSS define las variables Quasar (--q-primary #c9a227, --q-dark...) en :root, CSS_GLOBAL los estilos; shared=True aplica a todas las páginas sin UI global. app.py ya NO llama al tema; webui.py lo llama a nivel de módulo. Verificado corriendo EXACTO `python -X utf8 app.py` (3 rutas 200, CSS dorado presente en la página). 6/6 baterías PASS.
- **REVERT MIGRACION WEB ROTA (2026-08-13)**: el intento de consolidar webui.py -> paquete `web/` (webui.py reescrito con 291 líneas + variables indefinidas ENCH_COLORS/RESUMEN, pagina_salsas a medio terminar con `pass`, web/paginas/* SIN decoradores @ui.page y código a nivel de módulo) dejó la web en 404 en TODAS las rutas. `app.py` pasó a importar `from web import set_config` (que no registraba rutas) en vez de `webui`. FIX: `git checkout HEAD -- app.py webui.py` — se restauró el estado que funcionaba. VERIFICADO: `python -X utf8 app.py` + Invoke-WebRequest -> `/` 200, `/pesca` 200, `/recursos/piedra` 200, `/salsas` 200, `/buscar` 200. GOTCHA: había un server viejo (roto) colgado en el puerto 8080 dando el 404 que el usuario veía — matar con `Get-NetTCPConnection -LocalPort 8080 | Stop-Process` antes de re-probar. El refactor DRY de `utilidades.py`/`menus.py` (6/6 baterías PASS) SÍ quedó. El paquete `web/` sigue en el working tree (untracked) pero NO se importa desde app.py; decisión del usuario: rediseñar/descartar. Lección: migrar de a UNA página por vez con smoke test entre paso y paso, nunca reescribir todo de una.
- **FIX SPINNER "Consultando mercado..." PEGADO (2026-08-13)**: en `pagina_detalle_pez` la columna del spinner se creaba pero NUNCA se borraba al llegar los datos — quedaba "Consultando mercado..." permanentemente arriba en TODOS los detalles de pez. Fix: guardar la columna en `cargando` y llamar `cargando.clear()` tras `await run.io_bound(_datos_pez, ...)`. Verificado: /pesca/Perca%20de%20Albion 200.
- **FIX BOTON DOBLE FLECHA (2026-08-13)**: el botón "← Volver a pesca" mostraba 2 flechas (ícono `arrow_back` + carácter `←` en el texto). Fix: el texto pasa a "Volver a pesca" sin la flecha unicode (queda solo el ícono). Nuevo helper `_boton_volver(ruta, etiqueta)` en webui.py.
- **FASE 2 WEB — RECURSOS IMPLEMENTADOS (2026-08-13, sin commit)**: `/recursos/{tipo}` deja de ser placeholder. Replica fiel de la consola (`ver_recurso`/`_ver_detalle_recurso` de menus.py): listado de tiers (T2/T3 = una entrada "todo"; T4+ = crudo y refinado separados) con color de tier + badge, y `/recursos/{tipo}/{tier_key}/{modo}` con tabla generica (`_render_tabla_generica`: Ciudad | item(s) | Actualizado por columna, verde/rojo, "—" sin datos), panel Volumen 7 dias y Resumen de mercado (modo "todo" = grid alineada crudo Y refinado juntos; modos crudo/refinado = filas clasicas). `_datos_recurso` replica la lógica de datos de la consola (encantamientos .1-.4 solo T4+, get_prices/get_history_raw). `/salsas` y `/buscar` siguen placeholder pero con botón "Inicio". Botones de volver agregados a `/pesca` y `/recursos` ("Inicio"/"Volver a {par}"). VERIFICADO en puerto 8081 (sin tocar el server del usuario en 8080): /, /pesca, /recursos/fibra, /recursos/fibra/T2/todo, /recursos/fibra/T4/crudo, /salsas, /buscar -> 200. 6/6 baterías PASS. py_compile OK. LSP errors restantes son falsos positivos preexistentes (lambdas GenericEventArguments, tipado run.io_bound).
- **REGLAS SESION (2026-08-13, usuario)**: (1) NUNCA matar el server del usuario en puerto 8080 ni arrancar el propio en ese puerto — para smoke tests usar puerto 8081 (`_smoke_webui.py` temporal con set_config + ui.run(port=8081) y borrarlo). (2) El modelo de opencode para modos plan/build cambió a `opencode/deepseek-v4-flash-free` (config global ~/.config/opencode/opencode.json). (3) La web está funcionando de nuevo y el usuario la usa; "lo único que sirve es lo de pesca" (recursos recién implementados, salsas/buscar pendientes).
- **WEB FLASK COMPLETA + BUSCADOR (2026-08-14)**: la web nueva (Flask+HTMX+Tailwind en 8081) ya cubre todas las secciones: dashboard 3 tarjetas (Pesca/Recursos/Salsas, buscar vive en el header), pesca, recursos, salsas y buscador global (`/buscar` con HTMX en vivo, detalle por tipo: arma 5 paneles, diario Vacío/Lleno, simple 5 calidades; `/buscar/tabla`, `/buscar/item/<id_base>`). Config con 2 QRs (local + túnel Cloudflare). GOTCHA procesos: los procesos hijos que lanzo con mis comandos MUEREN al terminar mi sesión (el PowerShell se cierra y mata a sus hijos); para que el server sobreviva hay que lanzarlo DESACOPLADO vía WMI (`Invoke-CimMethod Win32_Process Create`) — así Flask queda vivo como proceso independiente (PID propio, responde 200 tras mi sesión). cloudflared (túnel) se lanza igual y la URL cambia en cada reinicio (guardar en tun_url.txt). Túnel actual: `https://shoes-disco-voluntary-transcription.trycloudflare.com`.
- **LIMPIADO NICEGUI + PLAN LANZADOR (2026-08-14, Fase 1 de empaquetado)**: borrado todo lo de NiceGUI (app.py, webui.py, estilo.py, paquete web/) porque la web oficial es Flask. Creados requirements.txt (Flask, qrcode, Pillow, requests, rich, pystray) y version.txt (1.0.0). Smoke 6/6 rutas OK. PLAN APROBADO por el usuario: (F1) limpieza + commit; (F2) lanzador `lanzador.py` tkinter+pystray con estado del server, iniciar/detener web, abrir navegador, QR, Cloudflare on/off, consola, buscar actualizaciones, minimizar a bandeja; wizard primer arranque (carpeta + acceso directo escritorio/Inicio/ambos/ninguno + ¿navegador o instalar PWA?); (F3) PWA (manifest+sw.js); (F4) botón buscar actualizaciones vs GitHub releases + version.txt; (F5) PyInstaller onedir portable (Python incluido, "Docker sin serlo") + zip + prueba pendrive; (F6) ESTADO/README, commit+push, release v1.0.0. Todo portable, el usuario final NO instala nada.
- **LANZADOR + PWA + ACTUALIZACIONES + PORTABLE COMPLETADOS (2026-08-14, Fases 2-5)**: (F2) `lanzador.py` (tkinter+pystray): estado server verde/rojo, iniciar/detener web, abrir navegador (o PWA), QR, Cloudflare on/off (captura URL desde tun.log), consola (exe aparte), buscar actualizaciones (GitHub releases vs version.txt), minimizar a bandeja. Wizard primer arranque (carpeta + acceso directo vía wscript.exe VBS — cscript NO existe en esta PC + navegador/PWA). GOTCHA: procesos hijos del lanzador mueren si el lanzador se cierra (por diseño: el lanzador los posee). (F3) PWA: `static/manifest.json` + `static/sw.js` + rutas /manifest.json y /sw.js + registro en base.html. (F4) `buscar_actualizaciones()` valida releases de GitHub, descarga zip a actualizaciones/. (F5) `build.py` genera con PyInstaller onedir: `dist/AlbionHelper/AlbionHelper.exe` (GUI + `--server`), `AlbionHelperConsole.exe` (consola), datos editables junto al exe (templates, static, albion_config.json, catalog.json, version.txt, cloudflared.exe). VERIFICADO: exe --server 6/6 rutas + buscador lee catalog.json junto al exe; consola exe arranca; GUI exe abre/cierra; ZIP portable (53.8 MB) descomprimido en carpeta limpia (simula pendrive) responde 200. Claves frozen-aware: BASE_DIR = dirname(sys.executable) en flask_app/catalogo/albion_helper cuando sys.frozen.
- **UX/UI ESTILO ALBION (2026-08-15)**: (1) Fix terminales fantasma: todo subprocess.run (tasklist/netstat/taskkill/wscript) con creationflags=CREATE_NO_WINDOW — antes cada polling (estado server cada 3s) abria una consola fantasma. (2) Textos del lanzador en espanol neutro/latino (sin voseo). (3) TEMA ALBION en web: fondo radial calido #3a2a35→#1a1410, titulos degradado dorado (.titulo-oro #f5d576→#e8b84a + text-shadow), bordes bronce #8a6a3a, hover ambar #e8a545, tipografia CINZEL empaquetada (static/fonts, @font-face, offline, ~39 KB), marco de item estilo juego (.item-marco: fondo #0d0d0d + borde bronce #5a4020 + halo ambar) en resultados y detalle del buscador, colores de rareza del item como borde de card. (4) Lanzador rediseñado oscuro Albion: tema clam custom (fondo #1a1410, paneles #241c16, títulos #f5d576), SWITCHES toggle (widget Switch canvas, verde #2ecc71 on) en vez de botones Iniciar/Detener para Servidor web y Cloudflare, wizard y QR oscuros. (5) AUTO-UPDATE con barra de progreso: _descargar usa thread + stream + iter_content + ttk.Progressbar; al terminar boton "Aplicar actualizacion ahora" (adiós al descomprimir manual); _aplicar_actualizacion hace backup de albion_config.json + lanzador_config.json, genera script oculto vbs->bat (espera 3s por _internal/ en uso, Expand-Archive sobre la carpeta, restaura config, relanza exe, se autoborra) y cierra el lanzador. (6) Fix bandeja: MenuItem("Abrir Albion Helper", default=True) → doble clic restaura la ventana. GOTCHA: ttk.Frame NO acepta parent.cget("bg") (TclError unknown option "-bg") — usar el color del tema directamente. VERIFICADO: smoke web 8/8 (incluye las 2 fuentes woff2 200), exe --server con tema calido + fonts, GUI empaquetado sin crasheos, sin procesos fantasma, auto-update simulado (zip v1.0.1 se expande y la config del usuario se preserva).
- **APP SIN LANZADOR + TUNEL EN /CONFIG (2026-08-19)**: decisión del usuario: abandonar el lanzador tkinter por ahora. Nuevo `app.py` = ENTRY del exe: levanta Flask (hilo daemon) + abre ventana tipo app MAXIMIZADA con pywebview (`import webview`, no `pywebview` — el paquete pip se llama pywebview pero el módulo es webview; GOTCHA). Al cerrar la ventana, `webview.start()` retorna → `_shutdown_server()` llama `/shutdown` → `_apagar_flask` hace `os._exit(0)` (apaga server y proceso). Control de túnel MOVIDO del lanzador a la web: flask_app.py gana `cloudflared_activo()`, `iniciar_tunel_func()` (regex `https://[a-z0-9-]+\.trycloudflare\.com` + poll 15s sobre tun.log) y `detener_tunel_func()`, más rutas `POST /tunel/start`, `POST /tunel/stop`, `GET /status`, `GET /shutdown`. `/status` y `/config` reportan el estado REAL (basado en `cloudflared_activo()`, NO en si existe tun_url.txt — fix a un bug donde una URL vieja mostraba "tunel activo" sin proceso). config.html reescrito: control del túnel con fetch + actualización dinámica de la URL y el QR internet (img src recargado via /qr?url=...), botón "Cerrar aplicacion" (fetch /shutdown), versión visible. build.py: exe principal ahora empaqueta app.py (windowed + hidden_import webview); AlbionHelperConsole.exe intacto. VERIFICADO: rutas 6/6 200 en exe empaquetado y en carpeta limpia (zip 52.8 MB descomprimido simula pendrive); túnel start→URL real→stop y shutdown funcionan en ambos; proceso del exe se cierra solo con /shutdown. GOTCHA dev: hay que matar servers viejos huérfanos en 8081 (dos procesos python escuchando el mismo puerto daban respuestas del código viejo).
- **FIX SELECCIÓN DE TEXTO (2026-08-19)**: pywebview trae `text_select=False` por defecto en `create_window` → el usuario no podía seleccionar texto en la ventana. Fix: `text_select=True` en app.py. Commit `580fd7a`. Rebuild + zip regenerado (52.8 MB) y verificado.
- **UX ACCESO PRIVADO/PÚBLICO + SWITCH EN VENTANA QR (2026-08-21)**: el usuario pidió eliminar la palabra técnica "Túnel" de toda la UI (un usuario común no la entiende) y agregar el switch on/off a la ventana de solo-QR. Texto visible NUEVO en toda la app: card 1 "ACCESO PRIVADO (red local · LAN o WiFi)", card 2 "ACCESO PÚBLICO (desde internet)", switch "Acceso público" con estados "Disponible/No disponible" y "Conectando..." mientras arranca; "Sin tunel activo" → "No disponible"; bandeja "Túnel: Activo/Inactivo" → "Internet: Disponible/No disponible". Los términos técnicos (tunel, cloudflared) quedan SOLO en código interno (ids `tunel-switch`, rutas `/tunel/*`). qr_solo.html ahora replica el control completo de config.html: switch Tailwind peer-checked + skeleton animate-pulse + JS set_cargando/actualizar_tunel/bind_acciones (fetch /tunel/start|stop, revierte si falla); poll_status cada 5s sincronizado con `url_actual` (ya no compara contra textContent, evita pisar el estado del switch). flask_app.py: CLOUDFLARED gana fallback a TEMP\cloudflared.exe cuando no existe junto al repo (mismo criterio que build.py; en el exe empaquetado siempre vive junto al exe) — antes el ciclo del túnel fallaba en dev con "cloudflared.exe no encontrado". VERIFICADO dev y exe empaquetado: /qr-solo 200 con switch+ACCESO PRIVADO/PÚBLICO y cero texto visible "Túnel" (regex sobre textos entre tags), /config igual; túnel start→URL real trycloudflare→switch checked+rotulo verde "Disponible"→stop OK. Zip regenerado (52.9 MB).
- **MODELOS OPENCODE RECONFIGURADOS (2026-08-21, fuera del repo)**: ~/.config/opencode/opencode.json actualizado — los IDs `opencode/deepseek-v4-flash-free` y `opencode/north-mini-code-free` YA NO EXISTEN en el catálogo de OpenCode Zen (causaba reports vacíos en los jueces). Nueva asignación SOLO con modelos FREE actuales de Zen (verificados en https://opencode.ai/docs/zen): global+x-preview-f-free (Ox Alpha), plan/sdd-design/sdd-propose/sdd-verify/jd-judge-a/review-risk=nemotron-3-ultra-free, build/sdd-apply/jd-judge-b/review-resilience=big-pickle, sdd-spec/sdd-tasks/jd-fix-agent/review-reliability=mimo-v2.5-free, sdd-explore/sdd-archive/sdd-init/sdd-onboard=nemotron-3.5-lightning-free, review-readability=hy3-free, gentle-orchestrator=x-preview-f-free. Jueces ciegos en FAMILIAS DISTINTAS (NVIDIA vs stealth) para revisión realmente independiente. Backup: opencode.json.bak-20260821. Los free de Zen son promos "por tiempo limitado": si un ID desaparece, reemplazarlo. Muse Spark Contributor Free SE EVITA (entrena con tu código). opencode NO hot-reloada config: requiere reinicio.
- **POLITICA GIT OBLIGATORIA (2026-08-19, regla global)**: el agente empujó commits sin preguntar y el usuario marcó la regla. AHORA: `git push` es SIEMPRE un comando aparte (nunca encadenado con `&&`/`;`/`|`), se pregunta al usuario y se espera confirmación explícita en el MISMO mensaje antes de ejecutarlo; un plan aprobado que mencione "push" NO es autorización. Regla escrita en AGENTS.md global. CAUSA RAÍZ del bypass (verificada en source de opencode): la tool bash pasa el comando COMPLETO como un solo string a `permission.assert` (`resources: [input.command]`) y el matcher `Wildcard.match` ancla el patrón con `^...$` — `git add -A && git commit -m "x" && git push` NO matchea `git push`/`git push *` y caía en el default `"*": "allow"`. FIX en opencode.json global: patrones nuevos `* git push`, `* git push *`, `git add * && git push`, `git add * && git push *` (todos `ask`), `git commit *` pasa a `allow` (el usuario validó add/commit directos). Verificado simulando `evaluate()` con el matcher exacto: push simple, push con args y push encadenado (con `&&`, con `|` y con `;`) → `ask`; commit sin push → `allow`. NOTA: opencode NO hot-reloads config — hay que reiniciar opencode para que aplique.
- **UX /CONFIG: switch compacto a la derecha + skeleton + botón apagado (2026-08-19)**: 3 mejoras pedidas por el usuario en /config. (1) Switch compactado `w-14 h-8`→`w-11 h-6` (thumb `w-5 h-5`, `translate-x-5`) y movido a la DERECHA: fila flex `justify-between` con etiqueta "Túnel de internet" + subestado (Activo/Inactivo) a la izquierda y el switch a la derecha. (2) SKELETON LOADING al activar: el arranque de cloudflared tarda hasta ~30 s (poll 15 s), así que al prender el switch se muestra `#url-skeleton` (2 barras `bg-zinc-700/60` con `animate-pulse` que imitan URL+QR), se BLOQUEA el switch (`sw.disabled = true` via `set_cargando()`), el rótulo dice "Iniciando tunel..." en ámbar, y al resolver se restaura URL+QR; si falla revierte a "Sin tunel activo". (3) Botón "Cerrar aplicacion" ahora con ícono de power (SVG inline, caminito de línea) + borde rojo `border-red-500/40` y hover `bg-red-500/10`, estilo botón real no texto plano. VERIFICADO en dev y exe empaquetado: config 200, url-skeleton/w-11/justify-between/svg/set_cargando presentes, túnel start→Activo→stop→Inactivo. Commit `a0ade1b` + push.

## Pendientes / ToDo
- [ ] CAMBIO SDD v2 (acordado con el usuario, preflight: interactive/both/force-chained/400):
  - [x] BASE: baterias de tests migradas de Temp a `tests/` (PR 1) — slice apply 1 completado y verificado (12 + 4 PASS)
  - [x] A: T8 en los 5 recursos (fibra/madera/cuero/mineral/piedra x crudo+refinado = 10 items) — slice apply 2 (PR 2), tests/test_recursos_t8.py PASS
  - [x] B: market_summary — resumen informativo sin recomendaciones (min/max, volumen, dia mayor venta, ingrediente, diferencia refinado) — slice apply 2 (PR 2), tests/test_market_summary.py PASS
  - [x] C: buscador global desde menu principal (ignora acentos, catálogo local catalog.json, listado seleccionable) — slice apply 3 (PR 3), tests/test_catalogo.py PASS + resumen_ciudad en test_market_summary.py
  - [ ] D: historial de precios (get_history existe sin uso) + favoritos — definir en detalle al llegar
  - [ ] E: cambio de servidor (west=America / east=Europa / asia=Asia; hoy fijo en constants.py)
  - Estado: propuesta sdd-propose LANZADA y ABORTADA por el usuario ("seguimos luego").
  - Retomar: relanzar sdd-propose con el prompt guardado en Engram topic `sdd/albion-v2/propose-prompt` (#157); estado en `sdd/albion-v2/estado-sesion` (#156).
- [x] Propuesta aprobada (reencuadrada: resumen informativo en vez de recomendacion, sin SQLite/IA)
- [x] Specs creadas (5: test-migration, resource-t8, market-summary, global-search, server-selection) en openspec/specs/
- [x] Diseño técnico materializado por el orquestador (design.md) — el sub-agente sdd-design fallo 2x (reporte vacio)
- [x] CAUSA RAIZ arreglada: agentes sdd-* usaban modelo north-mini-code-free (reportes vacios) → cambiados a deepseek-v4-flash-free en ~/.config/opencode/opencode.json (PENDIENTE: reiniciar opencode para aplicar)
- [x] Work unit 2 (A: T8 + B: market_summary) COMPLETADO y VERIFICADO (2026-08-01): 10 items T8 en albion_config.json, get_history_raw en api.py, market_summary pura en formatting.py, bloque de recomendacion picar/entero ELIMINADO de menus.py, tests nuevos (test_recursos_t8.py 6 PASS, test_market_summary.py 7 PASS); 4/4 baterias verdes
- [x] Nombres T8 verificados contra base de datos del juego (albioncore/albiondatabase): corregidos 5 (Cáñamo fantasma/Tela barroca, Cuero fortificado, Mármol/Bloque de mármol); 5 correctos desde el aplicador (madera blanca, adamantium, piel resistente)
- [ ] VALIDAR FIX RENDER (usuario): doble clic a albion_helper.bat (primer arranque, consola legacy) -> ya NO deben verse códigos `←[36m`; en Windows Terminal verificar que los truecolor (T6 naranja, colores de calidad) NO regresaron. Tras OK: commitear el fix (working tree sucio: Fase C completa + fix sin commitear)
- [ ] Probar la herramienta en la practica con pescados (usuario)
- [ ] Continuar con recursos: fibra, madera, cuero, mineral (usuario probara)
- [ ] `logo_test.jpg` sin trackear (decisión del usuario: commit o borrar)
- [x] Repo creado en GitHub publico (2026-08-01): herramienta-precios-farmer-albion
- [x] Skills de marketing instalados (copywriting + product-marketing desde coreyhaines31/marketingskills) — registro actualizado
- [ ] PLAN LANZADOR/EMPAQUETADO (2026-08-14, aprobado): F1 limpieza NiceGUI ✓ -> F2 lanzador tkinter+pystray ✓ -> F3 PWA ✓ -> F4 actualizaciones GitHub ✓ -> F5 PyInstaller onedir portable ✓ -> F6 commit+push+release v1.0.0+CodeGraph ✓ (commit 6aee1ad + release v1.0.0). Mejoras UX/UI estilo Albion (2026-08-15): terminales fantasma ✓, espanol neutro ✓, tema oscuro Albion web+lanzador ✓, switches ✓, auto-update con barra de progreso ✓, fix bandeja doble clic ✓ — commit pendiente.
- [ ] NUEVO FLUJO SIN LANZADOR (2026-08-19): exe abre ventana app maximizada (app.py + webview) ✓, túnel Cloudflare + QRs + Cerrar app en /config ✓, switch on/off con Tailwind ✓, fix selección de texto ✓, UX switch compacto a la derecha + skeleton + botón apagado ✓, build ✓, zip portable ✓, verificación punta a punta ✓, commits 580fd7a + ae3a5df + a0ade1b pusheados. Decisión del usuario: el lanzador tkinter se deja de lado (queda en repo como legado). PENDIENTE: confirmar con el usuario en la PC real que la selección de texto funciona, que el switch se ve bien y que el skeleton aparece al activar.
- [ ] FLUJO NUEVO LAUNCHER+BANDEJA (2026-08-19): app.py reescrito (ventana de inicio con 3 opciones + diálogo de cierre siempre + bandeja pystray + flag --server), ruta /launcher + templates/launcher.html ✓, botón "Cerrar aplicacion" eliminado de config.html ✓, build.py con pystray/PIL ✓, verificación dev ✓, build exe + zip regenerado (52.9 MB) ✓, verificación del exe (--server, /launcher con opciones, /config sin btn-cerrar) ✓. FIXES 2026-08-20: la app NUNCA abre navegador (webbrowser eliminado), consola visible (sin CREATE_NO_WINDOW), túnel con label dinámico + check nativo + anillo de color del ícono según estado real. REDISEÑO OPCIONES (2026-08-20): opción 2 "Solo servidor" = ventana SEPARADA de SOLO QRs (/qr-solo, sin menú) + server de fondo + bandeja ✓, opción 3 "Consola" = abre la consola y CIERRA todo lo demás ✓, ruta /qr-solo en flask_app.py ✓, segunda ventana pywebview oculta (hidden=True) ✓, bandeja "Ver QR" → ventana solo-QR ✓, --server = ventana solo-QR ✓, build + zip regenerado (52.9 MB) ✓. UX PRIVADO/PÚBLICO (2026-08-21): ventana /qr-solo ahora tiene el SWITCH del acceso público (mismo control que /config: skeleton + Conectando... + reversión si falla) ✓, texto "Túnel" ELIMINADO de toda la UI visible (Acceso privado/público, Disponible/No disponible, bandeja "Internet: Disponible") ✓, fallback cloudflared TEMP para dev ✓, build + zip regenerado ✓. PENDIENTE: prueba manual del usuario en la PC real (ventana launcher → 3 modos → diálogo de cierre → bandeja → switch acceso público on/off desde la ventana QR → salir). Commit pendiente.
- [ ] Probar el ZIP portable en otra PC (usuario) y el flujo de PWA desde el túnel (HTTPS)

## Decisiones recientes
- NUEVO FLUJO SIN LANZADOR v2 (2026-08-19): el exe ahora abre una ventana chica de INICIO (launcher) que pregunta qué hacer (app/solo servidor/consola); al cerrar la ventana de la app SIEMPRE pregunta (cerrar todo / minimizar a bandeja / cancelar) — decidido por el usuario para no asumir que la X = apagar el server. Bandeja pystray para volver/controlar el túnel. pywebview: el evento `closing` es CANCELABLE (handler devuelve False → `args.Cancel=True` en winforms); `destroy()` dispara el mismo evento, por eso un flag `_cerrar_programatico` salta el diálogo en cierres programáticos. js_api se expone como `pywebview.api.*`; métodos de window (hide/show/maximize/load_url/destroy) son thread-safe (Invoke al hilo GUI). MessageBoxW nativo vía ctypes para el diálogo (no depende de pywebview).
- FIX RENDER: opción 2 (sin dependencia de ANSI) elegida por el usuario — el render por filas usa la API nativa Win32 (LegacyWindowsTerm + AnsiDecoder) SOLO cuando console.legacy_windows AND la consola es real (GetConsoleMode OK); con VT/Unix/pipe -> ANSI actual. Causa raíz: commit 4b9973f (31 Jul) introdujo ANSI crudo con _RawControl; Rich nunca habilita VT. Se descartó forzar SetConsoleMode(ENABLE_VIRTUAL_TERMINAL_PROCESSING) porque seguiría dependiendo de ANSI (2026-08-05)
- Columna "Actualizado" por ciudad en las 3 tablas de detalle: timestamps de la API en dict paralelo `fechas` (no tocan el contrato int de prices/market_summary); frescura = max lexico de sell_price_min_date/max_date de la fila; formateador `antiguedad` en formatting.py (2026-08-02)
- Reseñas de detalle sin meta-discurso: RESENAS_DETALLE queda SOLO con lo informativo (como leer precios/colores), sin colas "datos objetivos"/"sin recomendaciones" (2026-08-02)
- Volumen 7 dias: columna de volumen con ancho dinamico en _formatear_historial (formatting.py) para que el Total (aun de 9+ digitos) quede alineado con las ciudades (2026-08-02)
- Tabla de recurso unificada: el detalle (modo "todo") junta crudo y refinado en UNA tabla con columna `Ciudad` unica (antes dos tablas apiladas con Ciudad duplicada) (2026-08-02)
- Encantamientos `.1-.4` solo desde T4 en recursos: T2/T3 no tienen versiones encantadas en Albion, el detalle las omite (flag `has_ench` en _ver_detalle_recurso) (2026-08-02)
- Header de detalle con color de paleta: componente unico `_panel_detalle(nombre, color, contenido)`; los 3 detalles usan el color del tier/encantamiento del item (sin negrita, tag fuera del dim) para coincidir exactamente con el color del listado del menu (2026-08-02)
- Detalles unificados: header Panel (nombre en borde + reseña adentro) y footer caja de hint via `_hint_detalle()` en los 3 detalles + caso sin-datos de salsas (2026-08-01)
- Salsas: layout "listado arriba" (opcion 2 elegida por el usuario como UX): panel header -> listado [1][2][3] -> receta de la seleccionada (dinamica) -> datos (tabla/grid/volumen) -> hint. Nuevo parametro `titulo_abajo` en `_menu_seleccion` (2026-08-01)
- Headers/footers unificados: nombre de seccion SOBRE el borde + reseña ADENTRO del panel; footer SOLO con el hint en caja, descripcion del item suelta (2026-08-01)
- ELIMINADO el "extra" del menu de salsas: era comparacion de rentabilidad (recomendacion velada, contradice "solo datos"); el DETALLE de cada salsa conserva el analisis completo (2026-08-01)
- Volumen 7 dias por ciudad rotulado "uds" (ej: "Martlock: 50,912 uds") (2026-08-01)
- market_summary SIN historial: el usuario descarto volumen_total / dia_mayor_venta / volumen_dia ("dato pasado, no sirve"); el resumen queda solo con min/max por ciudad, ingrediente y diferencia refinado; el volumen 7d SIGUE visible en los paneles de historial por ciudad del detalle; `sin_datos` ahora es `max_venta == 0` (2026-08-01)
- FIX post-verificacion: la app NO recomienda comprar/vender (el usuario decide segun su rol); los colores verde/rojo son solo posicion del precio (mayor/menor) y el resumen de mercado muestra la ciudad de cada extremo (2026-08-01)
- NOMBRE DEL REPO: "La Herramienta de Precios del Farmer en Albion" (slug: herramienta-precios-farmer-albion) — formula que el usuario aprobo: [que es herramienta] + [para que precios] + [para quien farmer] + [juego Albion]; espanol, no ingles (2026-08-01)
- Skills de marketing instalados en .agents/skills: copywriting + product-marketing (coreyhaines31/marketingskills, MIT) — metodo: git clone + copia (npx skills add quedo en modo interactivo) (2026-08-01)
- Diseño v2: get_server_base() en api.py, cache sin invalidacion (URL incluye servidor), market_summary() en formatting.py devolviendo dict, normalizar() con unicodedata, buscador plan B catalogo local, selected_server en config (2026-08-01)
- BASE v2 (migracion de tests): sys.path absoluto de Temp reemplazado por path derivado de `__file__` (`os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`) — los tests corren desde la raiz sin import relativo (no aplica `python -m`, solo script directo) (2026-08-01)
- CAMBIO v2 acordado: T8 recursos + regla ingredientes + buscador global (ignora acentos, API) + historial/favoritos + cambio de servidor (2026-08-01)
- API Albion tiene 3 servidores por subdominio: west (America), east (Europa), asia (Asia) — hoy fija a west (2026-08-01)
- SDD init hecho: openspec/ + Engram sdd-init/albion; sin pytest -> strict_tdd: false (2026-08-01)
- Protocolo anti-limbo global: ESTADO.md por proyecto + codegraph + verificar subagentes (2026-08-01)
- `.atl/` y `.codegraph/` en .gitignore (artefactos de tooling regenerables) (2026-08-01)
- Selector sin parpadeo: frame a buffer + reescribir solo filas cambiadas (2026-08-01)
- Barra de seleccion = color del tier, texto negro; oscuros -> blanco (2026-08-01)
- T6 dark_orange -> truecolor 208 (Rich degradaba a amarillo en 16 colores) (2026-08-01)
- Raiz sale solo con Esc; solo digitos visibles como atajo (2026-07-31)
- Navegacion unificada Esc/Enter/R; tecla 0 eliminada de submenus (2026-07-31)
- Despedida: "Que la plata te sobre!" (2026-07-31)
- Espanol neutro en la UI (usuario venezolano, no voseo) (2026-07-31)
- Cache 60s + backoff 429 en api.py (2026-07-31)

## Tests / verificacion
- Bateria en `tests/regression_fase2.py` (12 secciones: limpiar_pantalla, _mover_cursor, _menu_seleccion, E2E menu_principal, pausa, confirmar, hints, diferenciador, colores T6 truecolor)
- API: `tests/test_api_cache.py` (4 tests: cache TTL, 429 backoff)
- T8: `tests/test_recursos_t8.py` (6 checks: tier T8 en 5 recursos, 4 claves, prefijo T8_, pares, simetria T4/T6, nombres)
- Market summary: `tests/test_market_summary.py` (10 secciones: pez con datos + contrato de claves, tiburon sin ventas, ingrediente si/no, sin peticiones extra, integridad config "uso" raro/comun, tiburon T8 -> "Trofeo de tiburón", desempate por volumen, resumen_ciudad promedio/rango/cambio, resumen_ciudad sin datos -> None, _formatear_historial crudo con promedio·rango·cambio %)
- Antiguedad: `tests/test_antiguedad.py` (6 secciones: unidades s/min/h/d, bordes 59/60 s/min/h y 24 h, desfase futuro, invalidos, formatos Z/offset/datetime, frescura max lexico)
- Catalogo (buscador): `tests/test_catalogo.py` (9 secciones: normalizar, dedupe @1..@4, deteccion de tipo, limpieza "(parcialmente lleno)", tokens AND, limite 15, sin datos, sin red, descarga fallida)
- Correr desde la raiz: `python -X utf8 tests/regression_fase2.py && python -X utf8 tests/test_api_cache.py && python -X utf8 tests/test_recursos_t8.py && python -X utf8 tests/test_market_summary.py && python -X utf8 tests/test_antiguedad.py && python -X utf8 tests/test_catalogo.py`
- Origenes intactos en `C:\Users\DrFox\AppData\Local\Temp\opencode\` (rollback seguro: los originales no se tocaron; los nuevos archivos en tests/ son copias con el import de sys.path relativo a la raiz)
